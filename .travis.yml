language: php
php:
  - 5.5
  - 5.6
  - 7.0

services:
  - apc
  - redis-server
  - memcache
  - memcached

before_script:
  - composer update
  - phpenv config-add travis-php.ini

script:
  - mkdir -p build/logs
  - ./vendor/bin/phpunit

after_script:
  - ./vendor/bin/coveralls -v