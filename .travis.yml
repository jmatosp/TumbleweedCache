language: php
php:
  - 5.5
  - 5.6
  - 7.0

services:
  - redis-server
  - memcached

before_script:
  - composer update
  - sudo apt-get install php5-apcu
  - phpenv config-add travis-php.ini

script:
  - mkdir -p build/logs
  - ./vendor/bin/phpunit

after_script:
  - ./vendor/bin/coveralls -v